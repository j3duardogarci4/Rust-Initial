// Controller which make the question of the exam.
use std::io;
// ArrayAnsw contains the answer registered
// ArrayQuest deliver the question according the progress

fn main() {
    println!("Initiating Exam...!");
    // Number of question and display
    readFile();
    
    while () {
    
     println!("Please input your answer:");
     
    }   
    
    
    let mut guess = String::new();

    io::stdin()
        .read_line(&mut guess)
        .expect("Failed to read line");

    println!("You guessed: {}", guess);
}

// Load the questions in a new array
fn readFile() {
  
}

fn recordAnsw(ParamAnswer){
  ArrayAnsw[] = ParamAnswer
  return 0;
}

fn assetAnswer(){
  #match
}

}
